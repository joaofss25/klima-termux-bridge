# Dependências do instagram_bot.py (Python) — instale com:
#   pkg install rust binutils -y   (1x só — precisa pra compilar o pydantic-core)
#   pip install -r requirements.txt
# Isso é SEPARADO do bridge.mjs (Node.js/npm), que usa o package.json ao lado.
#
# V.0.2.32 (2ª tentativa): instagrapi>=3.0 puxa "curl_cffi", que tem um bug
# real no script de build dele — não reconhece 'armv8l' (o uname que o
# Termux devolve), então SEMPRE falha ao compilar, em qualquer Termux.
# Voltamos pra 2.1.5, a última versão antes do curl_cffi virar dependência
# (usa só `requests`, sem esse problema). Ainda depende do pydantic 2.x
# (via pydantic-core, Rust) — esse aqui compila BEM no Termux, só precisa
# do `pkg install rust binutils` acima antes do pip install.
instagrapi==2.1.5
requests==2.32.3
